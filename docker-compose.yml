version: '3.8'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3005:3000"
      - "5560:5555"
    environment:
      - DATABASE_URL=postgresql://postgres:postgres@db:5440/bookly
    depends_on:
      - db
    volumes:
      - .:/app
      - /app/node_modules
  db:
    image: postgres:16
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=bookly
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5440:5432"
volumes:
  pgdata: